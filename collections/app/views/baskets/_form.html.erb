<%= form_with(model: basket, local: true) do |form| %>
  <% if basket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(basket.errors.count, "error") %> prohibited this basket from being saved:</h2>

      <ul>
      <% basket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity, id: :basket_capacity %>
  </div>

  <div class="field">
    <%= form.label :fill_rate %>
    <%= form.number_field :fill_rate, id: :basket_fill_rate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
